// Code generated by Firebird. Edit freely - this file is yours.
package dto

import (
{{- if .HasTime }}
	"time"
{{- end }}
{{- if .HasUUID }}

	"github.com/google/uuid"
{{- end }}
)

// Update{{ .ModelName }}Input represents the input for updating a {{ .ModelName }}.
// All fields are optional (nil means no change).
//
// Fields excluded from updates:
{{- range .ExcludedFields }}
//   - {{ . }} (immutable or auto-managed)
{{- end }}
type Update{{ .ModelName }}Input struct {
{{- range .Fields }}
	{{ .Name }} *{{ .Type }} `json:"{{ .JSONTag }},omitempty"{{ if .Validation }} validate:"{{ .Validation }}"{{ end }}`
{{- end }}
}

// TODO: Add custom update logic or validation here
// Example:
//
// func (input *Update{{ .ModelName }}Input) Validate() error {
//     // Custom validation logic
//     return nil
// }
//
// func (input *Update{{ .ModelName }}Input) HasChanges() bool {
//     return {{ range $i, $field := .Fields }}{{ if $i }} || {{ end }}input.{{ $field.Name }} != nil{{ end }}
// }
